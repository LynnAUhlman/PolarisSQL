SELECT
sc.StatisticalCodeID,
sc.Description,
sc.OrganizationID,
o.Name

FROM
Polaris.StatisticalCodes  sc WITH (NOLOCK)

INNER JOIN
Polaris.Organizations o WITH (NOLOCK) ON
	o.OrganizationID = sc.OrganizationID
