SELECT
psc.StatisticalClassID,
psc.Description,
o.OrganizationID, 
o.Name,
o.DisplayName

FROM
Polaris.PatronStatClassCodes psc WITH(NOLOCK)

INNER JOIN
	Polaris.Organizations o WITH(NOLOCK) ON
		o.OrganizationID = psc.OrganizationID
